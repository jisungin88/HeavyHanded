#pragma once

#include "CoreMinimal.h"

/**
 * 코어 루프(페이즈 · 타이머 · 적재 · 탈출 판정)와 런 진행(팀 골드 · 참가자 명단) 로그 카테고리.
 *
 * 런 진행을 따로 가르지 않는 이유는 둘이 같은 이야기의 앞뒤이기 때문이다 —
 * "왜 3명으로 시작했나" 는 명단(런)에서 나오고 "얼마를 벌었나" 는 적재(코어 루프)에서 나온다.
 * 카테고리가 갈리면 그 인과를 한 필터로 못 따라간다.
 *
 * [왜 헤더에 EXTERN 인가] 기본은 .cpp 안의 DEFINE_LOG_CATEGORY_STATIC 이다 (문서 07 테스트).
 *   코어 루프는 GameMode(구동) · GameState(진리원) · 적재존이 한 사건을 나눠 처리한다.
 *   "이 판이 왜 Escape 로 넘어갔나" 를 따라가려면 셋의 로그가 한 필터에 잡혀야 한다.
 *   노획물이 LogLoot 을, AI 가 LogGuardAI 를 EXTERN 으로 둔 것과 같은 이유다.
 *
 * [무엇을 찍나] 페이즈 전이는 전부 남긴다. 한 판에 대여섯 줄이라 양이 문제되지 않고,
 *   버그 신고가 "갑자기 결과 화면이 떴다" 로만 오기 때문에 전이 이력이 유일한 단서다.
 *   전이가 거부된 경우(권위 없음 · 중복 요청)도 사유와 함께 남긴다.
 */
DECLARE_LOG_CATEGORY_EXTERN(LogHeist, Log, All);
