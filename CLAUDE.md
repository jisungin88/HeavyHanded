# HeavyHanded — 작업 규약

기획명 **Sneakers**(가칭) / 리포 **HeavyHanded**.
2~4인 온라인 협동 물리 운반(하이스트) 게임. **Unreal Engine 5.4, Listen Server, 런타임 모듈 1개.**

---

## 0. 문서 원본

**규칙의 원본은 Notion 이다. 리포지토리에는 문서를 두지 않는다** (`docs/README.md` 참조).
규칙이 바뀌면 Notion 을 고치고, 이 파일은 그 요약본으로 따라간다.

| 무엇 | 어디 |
|---|---|
| 기술설계서 (규약 08종 + Systems 07종) | Notion — `https://app.notion.com/p/3b2aee16de23803fbeb2fea9538ee172` |
| 기술설계서 로컬 사본 (마크다운 15개) | `C:\Users\user1\Downloads\기술설계서\documents\` |
| **기획서** `HEAVY_HANDED_기획문서_v2` | `C:\Users\user1\Desktop\A\HEAVY_HANDED_기획문서_v2.docx` (원본은 Google Docs `1hV-j2Ggto2ok_hPdeDU6aGzb8uIYdXEt`) |

- 규약 문서: `01 아키텍처` `02 네트워크` `03 C++/BP` `04 데이터` `05 컨벤션` `06 협업` `07 테스트` `08 플러그인`
- 시스템 문서: `코어 루프` `소음·경계도` `노획물 물리` `플레이어·GAS` `경비 AI` `UI·HUD` `세션·로비`

**판단이 갈리면 이 파일이 아니라 원본을 확인한다.** 이 파일은 요약이라 항상 뒤처질 수 있다.
**기술설계서와 기획서가 어긋나면 기획서가 위다** — 기술설계서는 기획서를 구현으로 옮긴 것이다 (12절 참조).

---

## 1. 게임 기획 요약 (기획서 v2)

### 컨셉

> 무거운 걸 조용히 옮겨야 하는 4인조 강도단이, 경찰이 오기 전에 밴에 짐을 싣는다.

**총이 없다. 싸울 수 없다.** 가진 무기는 조용히 움직이는 것 하나뿐.
**값비싼 물건일수록 무겁고, 무거울수록 시끄럽다 — 탐욕과 침묵은 양립할 수 없다.**

핵심 재미 3기둥 — ① **소음 관리**(조작 자체가 리스크) ② **탐욕의 대가**(가치=무게=소음)
③ **터지는 순간의 장르 전환**(경보 = 잠입 → 90초 도주)

### 코어 루프

```
[은신처] 역할 선택 · 정산 · 장비 구매 · 스킬 강화 · 팀원 구출 · 목표 선택
   ↓
[준비 45초] 드론 보유 시 사전 정찰 (바로 시작 가능)
   ↓
[본 작업 7~9분] 건물 외부/지하주차장 시작 → 진입로 탐색 → 잠입
   → 노획 → 물리 운반 → 밴 적재 → "금고 하나만 더?" (경계도 상승 중)
   ↓
[탈출] 시간 만료 또는 경보 카운트다운 종료
   → 전원 승차 → 정산 / 미승차 → 체포(다음 작업 관전) + 해당 인원 노획물 몰수
```

**진입 규칙** — 진입로는 창문·뒷문·환풍구·옥상 등 장소별 복수 배치. **잠긴 문은 내부에서만 개방** —
먼저 진입한 인원이 동료의 길을 열어주는 구조로 초반 협력을 유도한다.

**승패** — 작업 성공 = 제한시간 내 목표 금액 달성 + **최소 1인 승차** / 최종 성공 = 3개 장소 통과 /
실패 = 목표 미달 또는 전멸 (실패한 장소를 처음부터 재시작, 구매 장비는 유지)

### 장소 3곳

| 순서 | 장소 | 성격 | 주력 위협 | 목표 금액 | 제한시간 |
|---|---|---|---|---|---|
| 1 | 저택 | 넓은 방 + 정원. 도주로 많음 | 경비견, 삐걱거리는 마루 | $50,000 | 7분 |
| 2 | 박물관 | 전시실 다층. 좌대·유리관 | 레이저 센서, 감시 카메라 | $120,000 | 8분 |
| 3 | 은행 | 폐쇄적 금고층. 통로 좁음 | 압력판, 자동 셔터, 무장 경비 | $250,000 | 9분 |

**은신처**는 작업 사이의 상시 허브 — 수익 정산(개인 기여도) · 장비 구매 · 스킬 강화(수치만, 신규 스킬 없음) ·
팀원 구출(비용 ↔ 장비 예산 트레이드오프) · 목표 선택.

### 소음 → 경계도 (3장)

| 행동 | 등급 | 경계도 | 반응 반경 |
|---|---|---|---|
| 걷기 / 앉아서 이동(Ctrl) | 없음 | 0 | — |
| 뛰기(Shift 홀드) · 물건 충돌 | 소 | +1% | 8m |
| 물건 떨어뜨리기 | 중 | +5% | 15m |
| 물건 던지기 | 대 | +10% | 30m |
| 트랩 작동 | 대 | +15% | 30m |
| 파손형 파괴 / 중량형 낙하 | 특대 | +25% | **전 구역** |
| 대형 금고 절단 | 특대(지속) | +3%/초 | **전 구역** |

**경비 인지 게이지** — 반응 반경 안 경비·경비견·감지 장치 머리 위에 차오른다. 차는 동안
**플레이어에게 시각 경고가 떠서 숨거나 멈출 유예가 생긴다.** 가득 차면 조사 상태로 전환.

**경계도 4단계 — 이진 판정(발각=즉시 실패)은 쓰지 않는다.** 4인 협동에서 1인 실수로 전원이 즉시 처벌받으면 좌절감만 남는다.

| 단계 | 게이지 | 상태 | 회복 |
|---|---|---|---|
| 평온 | 0~33% | 정상 순찰 | — |
| 의심 | 34~66% | 소음 지점 이동, 순찰 속도 +30% | 30초 무소음 |
| 경계 | 67~99% | 경비 2명 증원, 일부 출입구 셔터 폐쇄 | 60초 무소음 |
| **경보** | 100% | 경찰 출동까지 **90초**, 전 경비 추격 | **회복 불가** |

> **경보는 실패가 아니다.** 90초 내 밴 탑승 시 성공 처리되며, 이 구간이 게임의 클라이맥스다.

### 플레이어 (4장)

**조작** — WASD 이동(물리 캡슐, 플레이어끼리 충돌) / Ctrl 앉아 이동(소음 0, 속도 50%) /
E 상호작용(집기·장치·동료 복구) / Q 놓기 / 좌클릭 홀드 던지기 / E 협력 캐리 / 1·2·3 스킬

**상태** — 과적(중량형 1인: 속도 30%, 점프 불가) · 다운(무장 경비 접촉 / 경비견 3회 / 낙하 → **동료가 5초간 복구**) ·
체포(다운 상태로 밴 출발 또는 미승차 → 다음 작업 시작 시 복귀 또는 은신처에서 비용 구출)

### 역할 4종 × 4슬롯 = 16 스킬 (4-1장)

실루엣만 보고도 역할을 구분할 수 있게 오버워치식으로 **4개 역할 고정**.
전투가 없으므로 역할 강제(2-2-2) 대신 **특정 순간에만 특정 역할이 필수가 되는** 구조로 다양성을 유도한다.
슬롯은 **패시브 / 액티브 A / 액티브 B / 팀 시너지**.

| 역할 | 패시브 | 액티브 A | 액티브 B | 팀 시너지 |
|---|---|---|---|---|
| **브루트** 힘 | 강한 등 — 중량형 속도 페널티 절반 | 벽 뚫기 돌진 | 동료 투척(상층·환기구 재배치) | 원맨 캐리 — 60초간 중량형 혼자 정상 속도 |
| **고스트** 이동 | 경량 착지 — 낙하 소음 없음, 속도 +30% | 그림자 이동(압력판 무시) | 시간 늦추기(3초, 본인만) | 그래플 라인 — 두 지점 연결 통로 |
| **오라클** 분석 | 상시 스캔 — 근처 노획물 가격·무게 표시 | 시야각 투영(팀 공유) | 소원의 나침반 — 최고가 아이템 방향 | 전 구역 스캔 — 경비·트랩·레어템 10초 |
| **미믹** 기만 | 발소리 위장 — 뛰어도 걷기 등급 | 경비 변장 | 바디 스왑 | 카메라 루프 — 카메라 20초 정지 반복 |

**전 역할 공통 소모품 슬롯**(별도 1개) — 전원 카드보드 박스: 팀 전체 5초간 완전 은폐.

> **톤 기준** — 과장된 연출은 "그럴듯한 특수 장비/능력" 선까지만. 완전히 만화적인 스킬
> (자석 손, 고무줄 팔, 거대화 물약)은 이번 버전 제외, 추후 시즌/코스메틱 후보로 보류.

### 노획물 (5장) — 특성 3종 + 경보 연동형, 장소별로 메시만 교체

| 특성 | 규칙 | 저택 / 박물관 / 은행 | 협력 |
|---|---|---|---|
| **중량형** | 1인 시 속도 30%, 점프 불가. 낙하 소음 특대 | 중형 금고 / 석상 / 현금 파렛트 | **2인 필수** |
| **파손형** | 충격 3회 시 파괴. **가치 0 + 소음 특대** | 도자기 세트 / 유화 액자 / 유리 케이스 서류 | 1인, 시야 차단 |
| **불안정형** | 굴러감. **기울기 60도 초과 시 내용물 유출** | 와인 랙 / 유물 항아리 / 동전 자루 | 1인, 팀 방해 |
| **경보 연동형** | 압력판에서 분리 시 **60초 카운트다운**, 실패 시 경계도 +50% | 벽 금고 / 전시 왕관 / 보안 상자 | 1인, 전력 질주 |

- **경보 연동형이 시그니처 아이템이다** — 들어올리는 순간 팀 전원의 계획이 무너지므로
  "지금 들어도 되는지"를 실시간 협상하게 만든다. **다른 물건을 대신 올리면 30초 → 90초로 지연.**
- **대형 금고**(전 장소 공통, 숨겨진 위치 1개 고정) — **점착폭탄($25,000) 없으면 개방 불가.**
  절단 중 경계도 +3%/초. 단일 노획물 최고 가치. 은신처에서 "이번 판에 절단기를 살 것인가"를 만든다.
- **파손 흐름** — 충격 누적 0~3 → `[Server]` 파괴 판정·가치 0 → `[Multicast]` 파괴 연출·사운드(**연출 전용**)
  → `[Server]` 소음 "특대" 발행 → 경계도 +25%

### 환경 방해 요소 (6장) — 동일 베이스 클래스, 장소별로 메시·파라미터만 변경

| 유형 | 저택 | 박물관 | 은행 |
|---|---|---|---|
| 주기 개폐 | 자동 커튼(시야 노출) | 전시실 셔터 | 금고층 방화문 |
| 순찰 위협 | 경비견(소음 반응 빠름) | 경비원(원뿔 시야, 손전등) | 무장 경비(**접촉 시 즉시 다운**) |
| 감지 장치 | 삐걱거리는 마루 | 레이저 그리드, 감시 카메라 | 압력판(**위 물건이 사라지면 반응**) |
| 강제 이동 | — | 수하물 컨베이어 | 문서 이송 튜브 |

### 장비 (7장, 은신처 구매) — **런 단위로만 유지. 영구 세이브 없음. 상태는 GameState 에 보관**

고무창 신발 $8,000(소음 -50%) · 대차 $20,000(중량형 1인 운반, 좁은 통로 불가) · EMP $10,000(카메라 15초) ·
미끼 $6,000(경비 20초 유인) · 응급 키트 $7,000(다운 즉시 복구 1회) · 정찰 드론 $4,000 · 점착 폭탄 $25,000
**신발·카트를 뺀 나머지는 전부 던져서 사용 가능.**

### UI (8장) — 아래 13절에 UI 담당자용으로 자세히

> **"최다 소음 유발자" 표시는 반드시 넣는다.** 결과 화면에서 책임 소재가 드러나는 것이 이 게임의 코미디를 완성한다.
> **사운드는 폴리싱이 아니라 기능이다.** 소음 SFX 는 개발 초반부터 임시본이라도 배치한다.

### 부록 — 소음 밸런싱 시트

플레이테스트로 채워 나가는 표. 초기값 기준선 — 액티브 스킬 쿨다운 45초 · 팀 시너지 180초 ·
경계도 자연 감소 -1%/초(30초 무소음 후) · 경보 예정 카운트다운 60초 · 경보 카운트다운 90초.
**이 시트가 그대로 `DT_NoiseProfiles` 다.**

---

## 2. 대전제 — 이 프로젝트의 버그는 조용하다

**크래시로 오지 않는다. 코드가 조용히 아무 일도 하지 않는다.**
소음이 안 나가면 "경비가 못 듣는다", 복제를 빠뜨리면 "클라에서만 안 된다",
BT 노드가 실패하면 "경비가 가만히 서 있는다" 로만 드러난다. 컴파일 에러도 로그도 없다.

여기서 따라오는 세 가지가 이 리포의 모든 규칙의 뿌리다.

1. **호스트에서 테스트하면 거의 모든 버그가 숨는다.** 반드시 PIE 2인 이상 + `Play As Listen Server` +
   `Run Under One Process` 해제로, **클라이언트 창에서** 확인한다. 클라 창에서 안 본 기능은 동작한다고 말하지 않는다.
2. **조용히 return 하는 지점마다 로그를 남긴다.** 특히 권위 체크 실패 · null 참조 · 데이터 조회 실패(태그, DataTable 행, Blackboard 키).
3. **눈으로 못 잡는 판정은 순수 함수로 떼어내 Automation Test 로 못박는다.**

---

## 3. 절대 규칙 (어기면 사고가 난다)

### 서버 권위

**모든 게임플레이 판정은 서버에서만 한다.** 소음 발행, 경계도, 인지 게이지, 파손, 노획물 가치,
운반 성립, 어빌리티 활성화, 적재·승차·탈출 판정이 전부 여기 해당한다.

| 판정 주체 | 쓸 것 |
|---|---|
| `UActorComponent` | `HasServerAuthority(this)` (`Shared/NetAuthority.h`) |
| 서브시스템 · 콘솔 명령 등 소유 액터 없는 쪽 | `HasServerAuthority(GetWorld())` |
| `AActor` | `AActor::HasAuthority()` |

**컴포넌트·서브시스템에서 `GetOwnerRole()` 을 직접 쓰지 않는다.**
클라에서 로컬 스폰된 비복제 액터는 `GetOwnerRole()` 이 `ROLE_Authority` 라 게이트를 그냥 통과한다.

**여러 사람이 호출하는 API 는 게이트를 호출부가 아니라 API 안에 둔다.**
(`UNoiseSubsystem` 은 클라에서도 인스턴스가 생기지만 발행 API 가 전부 조용히 무시된다.)

### 복제

- **상태는 언제나 `복제 프로퍼티 + OnRep`. `NetMulticast` 로 상태를 바꾸지 않는다.**
  Multicast 는 그 순간 접속한 사람에게만 간다 — 체포→관전→복귀가 있는 게임이라 실제 문제가 된다.
  `NetMulticast` 는 **상태를 남기지 않는 연출 전용**이고 그때도 `Unreliable`. GAS 안이면 GameplayCue 를 먼저 본다.
- **`ReplicatedUsing` 을 선언하면 `GetLifetimeReplicatedProps` 를 같이 연다.**
  `DOREPLIFETIME` 누락은 컴파일 에러도 안 나고 호스트에서는 멀쩡하다 — 이 리포에서 가장 나기 쉬운 사고다.
- **Server RPC 에는 `WithValidation`.** `_Validate` 는 **악의적 입력만** 막는다(검증 실패 = 접속 끊김).
  빈 문자열 같은 정상 범위의 잘못된 입력은 `_Implementation` 에서 조용히 무시한다.
- **RPC 이름에는 `Server_` / `Client_` / `Multicast_` 접두사.**
- AttributeSet 은 `DOREPLIFETIME_CONDITION_NOTIFY(..., COND_None, REPNOTIFY_Always)`.

### OnRep 재실행 패턴 (이 프로젝트의 시그니처)

부착 · 물리 토글 · 콜리전 프로파일 변경은 **복제되지 않는 로컬 호출**이다.
복제되는 것은 "누가 들고 있는가"라는 사실 하나뿐.

> **서버와 OnRep 이 같은 함수 하나만 부른다.**
> `ABaseCharacter::ApplyCarryState()` / `ALootBase::ApplyCarryState()` — 이름도 일부러 같다.

**순서가 중요하다: 물리를 먼저 끄고, 콜리전 프로파일, 그다음 어태치.**
런타임에는 물리 시뮬 중인 컴포넌트를 웰드 없이 부착할 수 없다.

### 포인터와 GC

**UObject 계열 포인터 멤버에는 예외 없이 `UPROPERTY()`.** 스타일이 아니라 크래시 방지 규칙이다.
빠뜨리면 GC 가 회수한 뒤 **몇 분 후 전혀 다른 코드에서** 터진다.

- 원시 포인터 대신 **`TObjectPtr<T>`**, 사용 전 **`IsValid()`**
- 큐·맵에 잠시 보관되는 참조는 **`TWeakObjectPtr<T>`**
- **`TWeakObjectPtr` 를 담은 구조체는 BP 에 노출하지 않는다** (수명 관리가 깨진다)

### 인코딩

**한글 주석이 든 `.h`/`.cpp`/`.inl`/`.cs` 는 반드시 UTF-8 **with BOM**, 탭 들여쓰기(폭 4), LF.**
BOM 이 없으면 MSVC 가 CP949 로 읽어 C4819 가 나고, 한글 바이트 끝이 `0x5C` 면 줄 이음으로 먹혀
**다음 줄이 통째로 주석이 된다.** UBT 는 `/utf-8` 을 안 붙여준다. 강제는 `.editorconfig` 가 한다.

### 수치 하드코딩 금지

**같은 값이 두 곳에 있으면 반드시 어긋난다.** 헷갈리면 값을 옮기지 말고 **참조하게 만든다.**
특히 `GetDefault<T>()` 는 절대 null 이 아니므로 `Settings ? Settings->X : 1.7f` 같은 폴백을 쓰지 않는다 —
그 리터럴이 클래스 기본값과 따로 논다.

---

## 4. C++ 와 BP 경계

> **C++ 는 "무슨 일이 일어났는가"를 정한다. BP 는 "그게 어떻게 보이는가"를 정한다.**

새로 BP 에 열까 고민되면 순서대로 묻는다:
① 틀리면 플레이어마다 결과가 달라지나 → C++ ② 없어도 게임이 성립하나 → BP
③ 아티스트·기획자가 반복해서 만질 값인가 → BP ④ 매 프레임/매 충돌마다 도나 → C++

| 영역 | C++ | BP |
|---|---|---|
| 서버 권위 판정 · 복제 · RPC · OnRep · 알고리즘 · 물리 계산 · 클래스 구조 | ✅ | ❌ |
| 밸런싱 수치 | 모양과 기본값만 | ✅ 실제 값 |
| 메시 · 머티리얼 · 사운드 · 파티클 · 몽타주 · 위젯 레이아웃 · 레벨 배치 | ❌ | ✅ |

**진짜 이유는 병합 비용이다.** `.uasset` 은 바이너리라 **에셋 충돌은 한쪽 작업이 그대로 소실된다.**
BP 는 C++ 클래스를 상속해 **값만 지정하는 껍데기**로 유지한다.

### 경계를 넘기는 장치

| 장치 | 방향 | 언제 |
|---|---|---|
| `BlueprintImplementableEvent` | C++ → BP | **연출 훅.** 가장 많이 쓴다. 판정이 끝난 뒤 호출, BP 는 그리기만 |
| `BlueprintAssignable` | C++ → BP | BP 가 구독. 판정이 끝난 결과만 |
| `BlueprintNativeEvent` | C++ → BP | BP 가 **동작 자체를 바꿔야** 할 때만. `ProcessEvent` 비용이 있다 |
| `BlueprintCallable` / `BlueprintPure` | BP → C++ | 판정은 호출된 C++ 함수 **안에서 끝난다** |

- **새 델리게이트는 C++ 전용 `DECLARE_MULTICAST_DELEGATE_*` 를 기본**으로 하고,
  판정이 끝난 결과만 `DECLARE_DYNAMIC_MULTICAST_DELEGATE_*` 로 연다.
  기준은 "누가 구독하는가"가 아니라 **"구독자가 판정에 참여할 수 있는가"** 다.
- **BP 훅에서 게임 상태를 바꾸지 않는다.** 훅은 클라에서도 돈다.
- `BlueprintPure` 는 부수효과 없는 조회에만 — pure 노드는 **연결된 곳마다 다시 실행된다.**
- **BT 구조(`BT_*`, `BB_*`)는 에셋, 판정 노드(`BTTask_*` `BTDecorator_*` `BTService_*`)는 전부 C++.**

### BP 에 열지 않기로 한 것

`FOnLootImpactSignature`, `FOnNoiseReported`, `UAlertComponent::GetNoiseContribution()`,
`HasServerAuthority`. **열어달라고 하기 전에 03 문서 5장의 이유를 먼저 볼 것.**

---

## 5. 코딩 컨벤션

- **PascalCase.** bool 은 `b` 접두사 (`bIsDead`).
- 클래스 접두사 — `A`(Actor) `U`(UObject) `F`(구조체) `E`(enum) `I`(인터페이스) `T`(템플릿). UHT 가 검사한다.
- **파일명은 접두사를 뺀 클래스명** — `ALootBase` → `LootBase.h/.cpp`
- 열거형은 `enum class ... : uint8`
- **`UPROPERTY`/`UFUNCTION` 에 `Category` 는 필수.** 계층은 `|` 로 (`"Loot|Throw"`).
- 노출 수준: **`BlueprintReadOnly` 가 기본.** `BlueprintReadWrite` 는 런타임 변경이 정말 필요할 때만.
- **값을 열면 제약도 같이 건다** — `ClampMin`/`ClampMax`/`Units`, 에셋은 `AllowedClasses`/`RequiredAssetDataTags`.
- **`.h` 에서 `#include` 최소화, 전방 선언 우선.** 전방 선언이 불가능해 include 했다면 **왜인지 한 줄 남긴다.**
- 주석은 `/** ... */`. public 클래스·함수·프로퍼티에 "무엇을 하는가" 한 줄.
  **"왜"까지 적어야 하는 세 경우** — ① 순서가 중요한 곳 ② 직관과 어긋나는 값·선택 ③ 실측으로 정한 임계값.
- `BlueprintCallable` 함수의 이름·시그니처를 바꾸면 **BP 노드가 끊긴다.** 커밋 메시지에 어느 BP 를 누가 고쳐야 하는지 적는다.

### 에셋 접두사

`BP_` `WBP_` `M_` `MI_` `DT_` `DA_` `BB_`/`BT_` `GA_` `GE_` `SM_` `SK_` `T_` `S_` `SC_` `NS_` `IA_`/`IMC_`

---

## 6. 폴더 구조

```
Source/HeavyHanded/
├── Public/     AI · Alert · Character · Core · Data · Equipment · Hazards ·
│               Interaction · Interfaces · Loot · Noise · Shared · UI
└── Private/    Public 과 같은 구조 + Tests/
```

- `Public/` 과 `Private/` 은 **같은 하위 구조**를 갖는다.
- **`Shared/` 는 여러 시스템이 같은 식을 써야 하는 것만.** 잡동사니 폴더가 아니다.
  현재 `NetAuthority.h`, `Gauge01.h` 두 개뿐이고 둘 다 "복사본이 갈라져 사고가 났던 것"이다.
  **`Shared/` 에 파일 추가는 사전 합의 사항.**

```
Content/HeavyHanded/    ← 우리가 만든 에셋만 여기
├── Art Audio Characters Core Data Equipment FX Hazards Input Loot Maps UI Vehicles
Content/Developers/<이름>/  ← 개인 실험. 정식 경로에서 참조하지 않는다
```

- **외부 에셋은 원래 폴더 구조를 유지한다.** `Content/HeavyHanded/` 아래로 옮기지 않는다.
- **폴더 이동·이름 변경은 반드시 언리얼 에디터 안에서.** 탐색기에서 옮기면 참조가 전부 깨진다.

---

## 7. 데이터를 어디에 두는가

| 저장소 | 무엇 | 위치 |
|---|---|---|
| **GameplayTag** | **식별자.** "이것이 무엇인가" | `Config/Tags/*.ini` |
| **DataTable** | 같은 모양이 여러 행인 밸런싱 수치 | `Content/HeavyHanded/Data/DataTables/` |
| **DeveloperSettings** | 전역 단일 튜닝 값 | Project Settings → Game |
| **BP 프로퍼티** | 개체별 값 | BP 서브클래스 |

**태그에 수치를 넣지 않는다.** `Noise.Loot.Throw` 는 이름일 뿐이고 값은 `DT_NoiseProfiles` 에 있다.

### GameplayTag

- 태그 본문은 **시스템별 `Config/Tags/*.ini`** 로 분리돼 있다(머지 충돌 격리).
  **`DefaultGameplayTags.ini` 에 직접 추가하지 않는다.**
- **`+` 접두사를 쓰면 안 된다.** 이 파일들은 config 계층이 아니라 개별 파일로 직접 읽힌다 —
  `+GameplayTagList=` 로 쓰면 **경고 없이 태그 0개**가 된다.
- `DevComment` 는 비워두지 않는다.
- C++ 에서 참조하는 태그는 `Core/HeavyHandedGameplayTags.h` 의 `HHTags` 에 **네이티브 선언**.
  문자열 조회는 오타를 컴파일러가 못 잡고 런타임에 조용히 실패한다.
- **남의 시스템 태그 파일은 담당자에게 먼저 알린다** (배정표는 아래 10절).

### DataTable / Settings

- `DT_NoiseProfiles` 의 **`RowName` 은 GameplayTag 이름과 같게 한다.** 없으면 부모 태그로 거슬러 올라간다.
- 새 DataTable 참조는 `TSoftObjectPtr<UDataTable>` + `RequiredAssetDataTags` 로 행 구조를 강제한다.
- 현재 Settings — `UNoiseSettings`(`DefaultNoiseSystem.ini`) · `UAlertSettings`(ini 미생성) · `UUISettings`(`DefaultUI.ini`)
- **런타임 부착 컴포넌트의 수치는 항상 Settings 로 뺀다.** 디테일 패널에 안 뜨므로 `EditAnywhere` 로 두면 아무도 못 만진다.
- `TMap` 설정에는 조회 래퍼를 붙여 안전한 기본값을 돌려준다(행이 지워지면 계수 0 이 된다).
- 소프트 참조가 기본. 로드 후에는 캐시하고, **로드 실패도 "해결됨"으로 기억**한다.

---

## 8. 시스템 구성과 통신

### 레이어와 참조 방향

```
L0 Settings ← L1 WorldSubsystem / L2 GameFramework / L3 Actor / L4 Component ← UI(읽기·구독만)
                          ↕ L5 Interface
```

1. **컴포넌트는 자기 소유 액터의 구체 타입을 알지 않는다.**
2. **월드 서비스는 구체 액터 타입을 모른다. 인터페이스로만 접근한다.**
3. **권위 판정은 `Shared/NetAuthority.h` 하나만 쓴다.**
4. **공유 계산식은 한 곳에만 둔다** (`Shared/Gauge01.h`).
5. **UI 는 시스템 상태를 읽고 구독만 한다. 쓰지 않는다.**

> 레이어 규칙을 어겨야 할 상황이 생기면 **어기지 말고 규칙을 고치자고 제안한다.**
> 예외를 한 번 허용하면 규칙이 아니게 된다.

### 통신 수단 5종 (여섯 번째를 늘리려면 01 문서 6장에 근거부터 적는다)

C++ 멀티캐스트(기본) · Dynamic 멀티캐스트(판정 끝난 결과) · 인터페이스 ·
GameplayTag/GameplayEvent · DataTable/DeveloperSettings

### 소유권

| 대상 | 소유자 | 왜 |
|---|---|---|
| `UNoiseSubsystem` | `UWorld` | 맵 전체가 공유하는 단일 서비스 |
| `UAlertComponent` | `AGameStateBase` (런타임 부착, 서버 전용) | 경계도는 저택 전체의 상태 |
| `UAbilitySystemComponent` · `UBaseAttributeSet` | `APlayerSessionState` | 체포→관전→복귀 시 폰은 죽어도 강화 수치는 유지 |
| `UPerceptionMeterComponent` | `AGuardCharacter` | 인지 게이지는 개체별 |

### 게임 흐름

**Prep(45s) → Heist(7~9분) → Escape(90s) → Result.** 단방향, 되돌아가지 않는다.
**경보는 실패가 아니라 90초 도주 페이즈의 시작이다.** 시간 만료도 경보도 둘 다 Escape 로 모이고 사유만 다르다.
로비/은신처는 별도 레벨이고 ServerTravel 로 전환한다(세션 파트).

---

## 9. 테스트 · 디버깅

- **Automation Test 는 조용히 깨지는 것만 테스트한다.** 화면 보면 아는 것은 안 한다.
  이름 규칙 `HeavyHanded.<시스템>.<대상>.<검증 내용>` — 검증 내용은 "무엇이 참이어야 하는가"로 쓴다.
  위치 `Private/Tests/`, 전체를 `#if WITH_DEV_AUTOMATION_TESTS` 로 감싼다.
- **테스트가 규칙을 복제해 들고 있으면 안 된다** — 그러면 프로덕션 코드가 아니라 자기 복사본을 검증한다.
  검증 대상을 순수 함수로 떼어내는 쪽이 맞다.
- 실행: `UnrealEditor-Cmd.exe <uproject> -ExecCmds="Automation RunTests HeavyHanded; Quit" -unattended -nullrhi`
- **CVar/명령 이름은 `hh.<시스템>.<대상>`.** 치트에는 `ECVF_Cheat` + `#if !UE_BUILD_SHIPPING` 가드.
  현재 — `hh.Noise.Debug` `hh.Ability.Debug` `hh.Alert.Set` (코어 루프 쪽에 `hh.Phase.*` `hh.Result.Show`)
- **로그 카테고리는 `DEFINE_LOG_CATEGORY_STATIC` 이 기본.** 여러 파일이 공유할 때만 헤더에 `EXTERN`(현재 `LogGuardAI` 하나).
  현재 — `LogNoise` `LogNoiseEmitter` `LogAlert` `LogGuardAI` `LogInteract` `LogHeavyUI`
- **반복 경로에서는 한 번만 찍는다** (물리 낙하 1회에 `OnHit` 이 5~15회 온다).
- 디버그 드로잉은 `#if ENABLE_DRAW_DEBUG` 로 감싼다. 전역 시스템은 CVar, 인스턴스가 여럿인 액터는 액터별 bool.

---

## 10. 협업

### 담당 영역

| 담당 | 영역 | git | 브랜치 | Tags 파일 |
|---|---|---|---|---|
| 지성인 | 소음 · 경계도, 코어 루프 | `jisungin` | `develop`, `feature/coreloop` | `Noise` `Alert` `Phase`(코어루프) |
| 이지은 | 세션 · 네트워크 | `LEEEJJJJ` | `develop` | `Phase` |
| 전영배 | 플레이어 조작 · GAS | `ybj0212` | `feature/PlayerAndSkills` | `Role` `Ability` `State` |
| 유정석 | 경비 AI | `badapple22` | `feature/AI` | `Guard` |
| 김민준 | 물리 · 아이템 | `kimminjun020220-netizen` | `feature/physics` | `Loot` `Equipment` |
| **오유석** | **UI/HUD, 환경 방해 요소** | `YUSEOKS2` | **`feature/uiux`** | `Hazard` |

`Cue.ini` `Event.ini` 는 공용 — 각자 자기 것만 추가한다.

### 브랜치

`main`(아직 사용 안 함) ← `develop`(실질 기준선) ← `feature/*`
**작업은 `develop` 에서 따서 `develop` 으로 되돌린다. 주 1회 이상 `develop` 을 자기 브랜치로 머지한다.**
새 브랜치 이름은 소문자.

### 커밋 메시지

```
type: 무엇을 했는지 한 줄

- 세부 변경 1
- 세부 변경 2
```

- 타입은 **`feat` `fix` `docs` `style` `refactor` `test` `chore` 일곱 개만.**
- **콜론 앞에 공백 금지**(`test :` ✗). **scope 쓰지 않는다**(`feat(loot):` ✗).
- 병합 커밋은 git 이 만든 메시지를 그대로 둔다.
- **BP 를 깨뜨리는 변경은 본문에 `[담당자] 어느 BP 를 고쳐야 하는지` 를 적는다.**

### Git 작업 순서 (에셋 소실 방지)

```
전체 저장(Ctrl+Shift+S) → 에디터 닫기 → git pull → 에디터 열고 작업
→ 전체 저장 후 에디터 닫기 → 커밋 → 푸시
```

**에디터가 켜진 채로 pull 하면 메모리의 구버전이 받아온 내용을 덮어쓴다.**
`git lfs install` 은 clone 전 각자 1회 필수. 파일 락(`lockable`)은 아직 켜지 않는다 — 채팅 공지로 운영.

### 경계를 넘는 작업

담당 경계는 **머지 충돌을 줄이기 위한 것이지 금지선이 아니다.** 담당자에게 먼저 알리고 고친다.
**사전 합의가 필요한 것** — 여러 영역에 걸치는 새 인터페이스 · `Shared/` 에 파일 추가 ·
`Config/Default*.ini` 수정 · `BlueprintCallable` 시그니처 변경.

---

## 11. 플러그인

`GameplayAbilities` `OnlineSubsystem` `OnlineSubsystemNull` `Niagara` `ModelingToolsEditorMode`(Editor 전용).
**전부 엔진 내장이고 서드파티는 0개다.**

- **에디터 전용 플러그인은 `TargetAllowList: ["Editor"]` 를 반드시 같이 넣는다.**
- **도입하지 않기로 한 것** — CommonUI(UMG 단독), Chaos Geometry Collection(메시 스왑 + Niagara),
  `bEnablePhysicsPrediction`, `OnlineSubsystemSteam`(추후), LFS 파일 락.
  **다시 제안하기 전에 08 문서 3장의 결정 이유가 아직 유효한지 확인한다.**
- 새 모듈을 `Build.cs` 에 추가하면 **어느 시스템 때문에 들어왔는지 주석으로 남긴다.**

---

## 12. 기획서 ↔ 현재 구현 — 어긋난 곳과 미착수

**여기 적힌 것은 "아직 안 된 것"이지 "안 하기로 한 것"이 아니다.**
이 중 하나를 건드리게 되면 기술설계서 쪽도 같이 고치자고 제안한다.

### 기획서와 실제로 다른 것 — 물어보고 정해야 하는 것

| 항목 | 기획서 | 현재 구현 |
|---|---|---|
| **작업 성공 조건** | 목표 금액 달성 + **최소 1인 승차** | `HeistOutcome::Evaluate()` — 목표 달성 + **전원 탈출**이 성공, 하나만 맞으면 **부분 성공**(3단계 등급). 기획서에 "부분 성공" 개념이 없다 |
| **부록 밸런싱 시트 vs 3장 소음표** | 3장: 물건 충돌 +1% / 떨어뜨리기 +5% / 던지기 +10%<br>부록: 뛰기 +1% / 낙하 +5% / **물건 충돌 +10%** | 부록 쪽 "물건 충돌"이 3장의 "던지기"를 가리키는 것으로 읽어야 맞는다. **`DT_NoiseProfiles` 를 채우기 전에 확정할 것** |
| **경보 예정 카운트다운 60초** | 부록에 있음 | 경보 연동형 노획물의 60초와 같은 값인지, 별개 개념인지 문서에 정리돼 있지 않다 |

### 미착수 (기획서에 있고 코드에 없다)

- **역할 4종 × 16 스킬** — 현재 어빌리티는 `GAB_Interact` `GAB_Throw` `GAB_DropItem` **3종, 역할 무관 공통**.
  `Config/Tags/Role.ini` · `Ability.ini` 에 태그만 있다. 캐릭터 메시 4종은 `Content/Developers/Doom/Mesh/` 에 있다(출처·라이선스 미문서화)
- **다운 / 체포 / 복구 5초** — `State.Downed` 미착수. **코어 루프의 탈출 판정 분기가 여기 막혀 있다**
- **경비 3종의 고유 동작** — `Dog`(3회 접촉) · `Armed`(즉시 다운)이 태그만 있고 구현 없음
- **환경 방해 요소 전부 (6장)** — 주기 개폐 · 감지 장치 · 강제 이동. `Hazard.ini` 에 태그만. **오유석 담당**
- **장비 7종 (7장)** · **은신처 상점 · 스킬 강화 · 팀원 구출** — 전부 미착수
- **노획물 미착수분** — 불안정형 유출(기울기 60도) · 경보 연동형 좌대 카운트다운 · 대형 금고 절단
- **장소 3곳** — `Maps/Mansion` `Museum` `Bank` 폴더만 있고 맵이 없다. 정식 레벨 2개(`LV_0_LoginTitle` `LV_1_Shelter`)도 아직 `Maps/Test/` 아래에 있다
- **비공개 방 참가 경로** — 방 코드는 생성되는데 입력 UI 도 검색 경로도 없어서 **아무도 못 들어간다**
- **UI 미착수** — 아래 13절

### 알아둘 함정

- **`Config/DefaultEngine.ini` 의 `GameDefaultMap` 이 아직 엔진 템플릿을 가리킨다.**
  지금 패키징하면 타이틀이 아니라 빈 맵으로 부팅된다. 에디터에서는 맵을 직접 열어 플레이하므로 드러나지 않는다
- **`Interaction` 트레이스 채널이 죽어 있다** — 채널과 프로파일은 정의돼 있는데 어빌리티는 `ECC_Visibility` 로 스윕한다.
  지금 채널을 바꾸면 노획물 집기가 통째로 죽는다
- **`Config/DefaultAlertSystem.ini` 가 없다** — `UAlertSettings` 가 C++ 기본값만 쓴다. Project Settings 에서 한 번 저장해 생성하면 **반드시 커밋할 것**
- **`HHTags` 네임스페이스가 비어 있다** — C++ 에서 태그를 조회하는 코드를 쓰게 되면 여기부터 선언한다

---

## 13. 현재 작업 컨텍스트 — `feature/uiux` (UI/HUD, 오유석)

| 항목 | 값 |
|---|---|
| 소스 | `Source/HeavyHanded/{Public,Private}/UI/` |
| 설정 | `UUISettings` → `Config/DefaultUI.ini` → Project Settings → Game → UI |
| 에셋 | `Content/HeavyHanded/UI/` (`Common` `HUD` `Hideout` `Lobby` `Result` `Icons` `Fonts`) |
| 시안 | 리포 루트 `ui_title.png` `ui_menu.png` `ui_ingame.png` `ui_shop.png` `ui_result.png` |
| 로그 | `LogHeavyUI` |

### UI 규칙

- **위젯 C++ 베이스는 `Abstract`.** C++ 이 구독·해제·데이터 바인딩을 하고 `WBP_*` 가 비주얼을 전부 만든다.
  C++ 클래스를 직접 스폰하면 화면에 아무것도 안 나온다.
- **UI 는 복제하지 않는다. 복제된 상태를 읽고 구독만 한다.**
- **색·폰트를 위젯마다 찍지 않는다.** 전부 `UUISettings` 토큰(`GetUIColor` / `GetUIFont`)을 본다.
  위젯은 **`PreConstruct` 에서** 토큰을 읽어 자기 색을 칠한다(디자이너에서도 돌기 때문).
  UMG 디자이너에 찍은 색은 그대로 구워져 토큰을 바꿔도 따라오지 않는다.
- **폰트 프로퍼티를 직접 읽지 않는다.** 비어 있는 값을 위젯에 꽂으면 Slate 가 LastResort 로 떨어져 글자가 네모가 된다.
  창구는 `GetUIFont()` 하나뿐이다.
- **색만으로 단계를 구분하지 않는다.** 경계도 위젯은 `GetAlertLevelText()` 를 항상 같이 띄운다.
- **클라에서 서버 전용 데이터를 읽지 않는다.** `UAlertComponent::GetNoiseContribution()` 은 클라에서 항상 비어 있다 —
  결과 화면은 `GetNoisiestPlayer()` 를 쓴다.

### 현재 클래스

`UAlertGaugeWidget`(훅 `OnGaugeUpdated` `OnLevelUpdated`) · `UPerceptionMeterWidget`(`OnPerceptionUpdated`
`OnMeterVisibilityChanged`) · `UDetectionGaugeWidget` · `UUISettings`
BP — `WBP_HUD` `WBP_AlertGauge` `WBP_PerceptionMeter` / 공통 `WBP_Button` `WBP_Card` `WBP_StatBar`

> `OnLevelUpdated` 는 **최초 바인딩 시에도 한 번 호출되며 이때는 `NewLevel == OldLevel`** 이다.

### 구독 대상

| 델리게이트 | 출처 |
|---|---|
| `OnAlertGaugeChanged` / `OnAlertLevelChanged` | `UAlertComponent` |
| `OnPerceptionChanged` | `UPerceptionMeterComponent` |
| `OnChatMessageReceived` | `AShelterPlayerController` |
| `OnLobbyPlayerCountChanged` | `AShelterGameState` |
| `OnPhaseChanged` · `OnLoadedValueChanged` · `OnBoardedChanged` + Result 조회 | `AHeistGameState` (코어 루프, 계약 공개됨) |

### 기획서 8장 — UI 요구사항 전체와 현황

| 화면 | 요소 | 현황 |
|---|---|---|
| **HUD** | 남은 시간, 현재/목표 금액, 소지 노획물, 경계도 게이지(4단계 색상) | 경계도 게이지만 있음. 나머지 미착수 |
| **플레이어** | 체력, 스태미나 | 미착수 (`UBaseAttributeSet` 에 `Health` 만 있고 스태미나 없음) |
| **스킬** | 액티브 A / 액티브 B / 팀 시너지 쿨다운, 공통 소모품 슬롯 | 미착수 (스킬 자체가 미착수) |
| **경비 인지 게이지** | 반응 반경 내 경비·경비견 머리 위 원형 게이지 | `UPerceptionMeterWidget` 있음 |
| **소음 피드백** | 소음 지점의 **화면 가장자리 방향 표시** — 누가 어디서 냈는지 팀 전원이 인지 | **미착수** |
| **상호작용** | 조준 대상 프롬프트, 2인 캐리 대기, 경보 연동형 카운트다운, 절단 진행률 | **미착수** |
| **준비 시간** | 45초 타이머, 역할 선택, 장비 구매, 드론 탐색 뷰 | **미착수** |
| **은신처** | 장비 상점, 스킬 강화, 팀원 구출, 목표 선택, 준비 완료 | **미착수** (`UI/Hideout/` 빈 폴더) |
| **결과** | 적재 목록, 획득 금액, 개인 기여도, **최다 소음 유발자**, 성공/실패, 재도전 | **미착수** (`UI/Result/` 빈 폴더). 코어 루프가 데이터를 전부 제공 중 |
| **로비** | 호스트/참가, 4인 슬롯, 준비 상태 | 세션 파트 위젯이 `Content/Developers/Lee/` 에 있음 |

> **"최다 소음 유발자"는 기획서가 "반드시 넣는다"고 못박은 항목이다.**
> 결과 화면에서 책임 소재가 드러나는 것이 이 게임의 코미디를 완성한다.
> 데이터는 `AHeistGameState::GetNoisiestPlayer()` 로 이미 나와 있다.

---

## 14. Claude 에게

- **한국어로 답한다.** 코드 주석도 한국어(그래서 BOM 이 필수다).
- **소스 파일을 새로 만들거나 수정할 때 UTF-8 BOM 을 유지한다.** 이게 깨지면 빌드가 이상하게 망가진다.
- **기획서 근거가 있는 수치에는 어느 장인지 주석에 같이 적는다.** 확실하지 않으면 적지 않는다.
- **규칙과 실제 코드가 어긋난 것을 발견하면 한쪽으로 정하자고 제안한다.** 어긋난 채로 두면 두 규칙이 생긴다.
- **남의 담당 영역 파일을 고쳐야 하면 먼저 알린다.** 특히 `Shared/`, `Config/Default*.ini`, 남의 `Tags/*.ini`.
- **커밋·푸시는 사용자가 요청할 때만 한다.**
- 문서에 `TODO:` 로 남아 있는 것이 많다. **그 항목을 건드리게 되면 해당 문서도 같이 고치자고 제안한다.**
